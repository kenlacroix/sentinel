//! `sentinel-analyzer` CLI entry point (stub for Week 4).

fn main() {
    eprintln!(
        "sentinel-analyzer v{} — Week 4 deliverable, not yet implemented",
        sentinel_analyzer::version()
    );
    std::process::exit(2);
}
