//! `sentinel-fuzzer` CLI entry point (stub for Week 3).

fn main() {
    eprintln!(
        "sentinel-fuzzer v{} — Week 3 deliverable, not yet implemented",
        sentinel_fuzzer::version()
    );
    std::process::exit(2);
}
